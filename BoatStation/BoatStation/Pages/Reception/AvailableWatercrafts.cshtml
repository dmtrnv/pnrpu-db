@page
@model BoatStation.Pages.Reception.AvailableWatercrafts

@{
    Layout = "Shared/_Layout";
}

<!DOCTYPE html>

<html lang="ru">
<head>
    <title>База плав. средств</title>
</head>
<body>
<h3>Имеющиеся плав. средства</h3>
<div>
    <table class="table-bordered">
        <thead>
        @foreach (var header in Model.Watercrafts.Headers)
        {
            <th>@header</th>
        }
        </thead>
        <tbody>
        @foreach (var row in Model.Watercrafts.Values)
        {
            <tr>
                @foreach (var value in row)
                {
                    <td>@value</td>
                }
            </tr>
        }
        </tbody>
    </table>
</div>
</body>
</html>