@page
@model BoatStation.Pages.Reception.ClientsBase

@{
    Layout = "Shared/_Layout";
}

<!DOCTYPE html>

<html lang="ru">
<head>
    <title>База клиентов</title>
</head>
<body>
<h3>База клиентов</h3>
<div>
    <table class="table-bordered">
        <thead>
        @foreach (var header in Model.Clients.Headers)
        {
            <th>@header</th>
        }
        </thead>
        <tbody>
        @foreach (var row in Model.Clients.Values)
        {
            <tr>
                @foreach (var value in row)
                {
                    <td>@value</td>
                }
            </tr>
        }
        </tbody>
    </table>
</div>
</body>
</html>